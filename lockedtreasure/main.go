package main

import(
    "fmt"
)

func main(){
    var times int
    fmt.Scanf("%d", &times)
    for i := 0; i < times; i++ {
        var bandits, perlock int
        fmt.Scanf("%d%d", bandits, perlock)

    }
}
